studiert(anton, if)
studiert(hagen, cv)
studiert(gunther, cv)
studiert(adele, if)
studiert(uta, cv)
studiert(anna, if)

student(anton)
student(hagen)
student(gunther)

studentin(adele)
studentin(uta)
studentin(anna)

a)
teams(Person, if)
teams(Person, cv)

Teams werden gebildet:
studiert(anton, if)
studiert(adele, if)
studiert(anna, if)

studiert(uta, cv)
studiert(hagen, cv)
studiert(gunther, cv)

b)
teams(Student, Studentin)

Gefundende Paare:
teams(anton, adele)
teams(anton, uta)
teams(anton, anna)

teams(hagen, adele)
teams(hagen, uta)
teams(hagen, anna)

teams(gunther, adele)
teams(gunther, uta)
teams(gunther, anna)

c)
teams(Person,if),teams(Person2, cv)

teams(anton,hagen)
teams(anton,gunther)
teams(anton,uta)

teams(hagen, anton)
teams(hagen, adele)
teams(hagen, anna)

teams(gunther, anton)
teams(gunther, adele)
teams(gunther, anna)

teams(adele, hagen)
teams(adele,gunther)
teams(adele, uta)

teams(uta,anton)
teams(uta,adele)
teams(uta,anna)

teams(anna,hagen)
teams(anna,gunther)
teams(anna,uta)

